<%= form_with(model: weight_history2) do |form| %>
  <% if weight_history2.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(weight_history2.errors.count, "error") %> prohibited this weight_history2 from being saved:</h2>

      <ul>
        <% weight_history2.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :weight %>
    <%= form.number_field :weight %>
  </div>

  <div class="field">
    <%= form.label :memo %>
    <%= form.text_field :memo %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
